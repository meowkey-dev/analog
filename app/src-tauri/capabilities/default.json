{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what the shell needs: the window it was given, and opening a link in the real browser.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url"
  ]
}
