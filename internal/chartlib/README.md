# Chart vendor asset

`plotly-basic.min.js` is `plotly.js-basic-dist-min@2.35.2`, copied from the
[npm package](https://www.npmjs.com/package/plotly.js-basic-dist-min/v/2.35.2).
The adjacent `LICENSE` is from that package. SHA-256:

```
138c2e81014b979dc00867a93da55b7605a17495ee78dd7afb433b7f021dfcfa
```

This directory is the single source for the server, Vite development server,
and CLI HTML/PDF export. The URL includes the version so it can be cached
indefinitely without changing older cards.
