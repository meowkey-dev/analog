# The conformance harness. pyproject.toml is gone with the Python implementation;
# this is all that is left of it.
[pytest]
testpaths = tests
markers =
    contract: asserts behaviour pinned by contracts/ or SPEC.md
