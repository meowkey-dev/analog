// Placeholder — replaced by WP3/WP4/WP4b. Present so the toolchain is verifiable
// before any UI work starts.
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <main style={{ font: "14px system-ui", padding: "2rem" }}>Analog</main>
  </StrictMode>,
);
